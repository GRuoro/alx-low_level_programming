#include <ctype.h>
#include "main.h"

int _islower(int c);
{
if (islower(c))
	{
	return (1);
	}
else
	return (0);
}

/**
 * main - check the code.
 *
 * Return: Always 0.
 */

int main(void)
{
_islower();

return (0);
}
